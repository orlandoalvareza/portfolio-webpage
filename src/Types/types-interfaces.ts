export interface PROJECT {
  image: string;
  title: string;
  description: string;
  skills: string[],
  githubLink: string,
  netlifyLink: string
}

export interface SKILL {
  icon: string;
  title: string;
  description: string;
}